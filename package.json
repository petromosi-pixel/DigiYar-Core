{
  "name": "digiyar-next",
  "version": "3.0.0",
  "private": true,
  "scripts": {
    "web": "npx serve www",
    "cap:sync": "npx cap sync",
    "cap:add": "npx cap add android",
    "cap:open": "npx cap open android"
  },
  "dependencies": {
    "@capacitor/core": "^7.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.0.0",
    "serve": "^14.2.4"
  }
}